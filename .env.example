# InferenceIndexer MCP server env config
# Copy to .env or export in the shell before running.

# Base URL of the InferenceIndexer API
II_API_BASE=https://api.inferenceindexer.ai

# Optional Bearer API key (raises rate limits). Get one at
# https://www.inferenceindexer.ai/for-agents
II_API_KEY=

# Optional first-party SSR secret (higher rate tier; for trusted/self-hosted runs)
II_SSR_SECRET=